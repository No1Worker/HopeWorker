// 将所有的apt插件库统一加入。
apply plugin: 'com.jakewharton.butterknife'
def librarys = rootProject.ext.dependencies
dependencies {
    annotationProcessor "${librarys.butterknifeApt}"
    annotationProcessor "${librarys.routerApt}"
}